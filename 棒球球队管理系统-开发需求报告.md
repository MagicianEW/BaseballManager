# 棒球球队管理系统 — 开发需求报告

> 用途：供干活 agent 编写完整球队人员管理与技术统计系统  
> 技术栈：JavaScript 后台（Node.js）+ 前端（React 或 Vue）

---

## 一、现有参考资料

### 1.1 现有开源项目参考

| 项目 | 地址 | 技术栈 | 值得借鉴的部分 |
|------|------|--------|----------------|
| `rscampbell333/baseball-scorekeeper` | GitHub，6星，React | React + Create React App | 比分界面布局、场上局面（垒上情况）可视化、打数记录表单 |
| `bgaudino/baseball-scoreboard` | GitHub，React | React | 实时比分板设计 |
| `christopherdonohue/baseball` | GitHub | 纯 JS | 打线卡片（Lineup Card）编辑器，纯前端实现 |
| `ComposableConsulting/diamond-score` | GitHub，Python+Flask | Python/Flask | 活球同步、喷绘图（Spray Chart）、投球计数统计 |

### 1.2 权威统计定义来源

- **Wikipedia: Baseball statistics**：`https://en.wikipedia.org/wiki/Baseball_statistics`
- **Wikipedia: On-base plus slugging (OPS)**：`https://en.wikipedia.org/wiki/On-base_plus_slugging`
- **MLB 官方规则**：`https://www.mlb.com/official-information/rule-book`

---

## 二、核心功能模块

### 2.1 球队与球员管理

- **球员档案**：姓名、球衣号、投打习惯（左右打）、位置、身高体重、出生日期、照片
- **球队档案**：队名、队徽、主场球场、成立年份
- **阵容管理（Lineup Card）**：先发9人排列、先发投手、替补席
- **位置定义**：投手(P)、捕手(C)、一垒手(1B)、二垒手(2B)、三垒手(3B)、游击手(SS)、左外野手(LF)、中外野手(CF)、右外野手(RF)、指定打击(DH)

### 2.2 比赛记录（核心）

#### 比分局面记录

- **场上局面（Base Situation）**：无人、占一二垒、满垒等7种局面
- **出局数**（0、1、2）
- **当前局数**（半局）
- **比分**（客队/主队分）
- **打席编号**：当前打者在本局是第几个打席
- **出局顺序**：当前出局数已记录的打者顺序（便于记录双杀等）

#### 打席事件记录（Play-by-Play）

每次打席结束需记录：

| 事件类型 | 子选项 |
|----------|--------|
| **打数结果** | 一垒安打、二垒安打、三垒安打、本垒打、牺牲触击、牺牲飞球、内野安打、双杀打、接杀、封杀、触身球、四坏球、三振、盗垒、盗垒失败、投手犯规、捕逸、补逸 |
| **打席附加** | 被接替（接替打者要记录）、打点、得分 |
| **跑垒附加** | 跑垒、盗垒、盗垒失败、夹杀、触杀 |

#### 投球记录（Pitch-by-Pitch）

- 每颗球的投球结果（好球/坏球/击出/触身球等）
- 球种（直球、曲球、滑球、变速球、指叉球等）
- 投球数（球数）：当前打席的 balls-strikes 计数
- 累计投球数（用于计算投手负担）

#### 换人操作

- 换投：记录先发投手 → 后援投手，记录投球局数、被安打、失分
- 换打：记录打序替换
- 守备换人：记录守备位置替换

#### 裁判记录

- 好球带设定（可选，记录误判）
- 场上裁判（主审、左审、右审等）

---

## 三、技术统计数据体系

### 3.1 打击统计（Batting）

| 统计项 | 缩写 | 计算公式 / 定义 |
|--------|------|----------------|
| 打数 | AB | At Bats — 有效打席（不含四坏保送/触身球/牺牲打/因跑垒死亡而结束的打席） |
| 得分 | R | Runs Scored — 跑回本垒得分数 |
| 安打 | H | Hits — 击出安全打总数（一垒安打+二垒安打+三垒安打+本垒打） |
| 打点 | RBI | Runs Batted In — 因该打者击球而跑回本垒的分数 |
| 四坏球 | BB | Base on Balls (Walks) — 获得四坏球保送数 |
| 触身球 | HBP | Hit by Pitch — 被触身球击中 |
| 三振 | SO | Strikeouts — 被三振数 |
| 盗垒 | SB | Stolen Bases |
| 盗垒失败 | CS | Caught Stealing |
| 牺牲触击 | SAC | Sacrifice Bunts |
| 牺牲飞球 | SF | Sacrifice Flies |
| 双杀打 | GIDP | Ground Into Double Play — 击出双杀打 |

**衍生打击率（高级）**

| 统计项 | 计算公式 |
|--------|----------|
| 打击率 BA | `BA = H / AB`（保留3位小数） |
| 上垒率 OBP | `OBP = (H + BB + HBP) / (AB + BB + HBP + SF)` |
| 长打率 SLG | `SLG = (1B + 2×2B + 3×3B + 4×HR) / AB` |
| OPS | `OPS = OBP + SLG` |
| OPS+（标准化） | `OPS+ = 100 × (OBP/lgOBP + SLG/lgSLG - 1)`，lg为联盟平均值 |
| RC（得分创造） | `RC = (H + BB) × TB / (AB + BB)` |
| ISO（纯长打率） | `ISO = SLG - BA` |

### 3.2 投手统计（Pitching）

| 统计项 | 缩写 | 计算公式 / 定义 |
|--------|------|----------------|
| 投球局数 | IP | Innings Pitched — 投球局数（每出局算1/3局） |
| 胜投 | W | Wins — 胜投 |
| 败投 | L | Losses — 败投 |
| 救援成功 | SV | Saves — 救援成功 |
| 中继成功 | HLD | Holds — 中继点 |
| 被打 | H | Hits Allowed — 被击出安打数 |
| 失分 | R | Runs Allowed |
| 自责分 | ER | Earned Runs — 自责分（扣除守备失误导致的失分） |
| 四坏球 | BB | Walks Allowed |
| 触身球 | HBP | Hit by Pitch |
| 三振 | SO | Strikeouts |
| 暴投 | WP | Wild Pitches |
| 投手犯规 | BALK | Balks |
| 投手犯规 | PB | Passed Balls（捕手漏球，进攻统计归捕手）|

**衍生投手率（高级）**

| 统计项 | 计算公式 |
|--------|----------|
| 自责分率 ERA | `ERA = ER × 9 / IP` |
| 每局被上垒率 WHIP | `WHIP = (H + BB) / IP` |
| 三振率 K/9 | `K/9 = SO × 9 / IP` |
| 保送率 BB/9 | `BB/9 = BB × 9 / IP` |
| K/BB 比 | `K/BB = SO / BB` |
| FIP（投手独立防御率） | `FIP = ((13×HR)+(3×(BB+HBP))-(2×SO))/IP + FIP常数` |
| xERA（期望ERA） | 基于击球质量（exit velocity, launch angle）的期望ERA |

### 3.3 守备统计（Fielding）

| 统计项 | 缩写 | 定义 |
|--------|------|------|
| 刺杀 | PO | Putouts — 协助出局数 |
| 助杀 | A | Assists — 助杀数 |
| 失误 | E | Errors — 失误数 |
| 双杀 | DP | Double Plays — 协助完成双杀数 |
| 捕逸 | PB | Passed Balls — 捕手漏接（归捕手） |
| 盗垒阻止 | CS% | Caught Stealing % — `CS / (SB + CS)` |

**守备率**：`FPCT = (PO + A) / (PO + A + E)`

### 3.4 跑垒统计（Baserunning）

- 盗垒 SB、盗垒失败 CS（见打击统计）
- 跑垒得分 R（归打击统计）
- 离垒过早（攻）
- 滑垒情况

### 3.5 比赛整体统计（Game Log）

每场比赛结束后自动汇总：

```
比分：客队 X - Y 主队
时长：X小时Y分
观数：Z人
```

---

## 四、界面设计要求

### 4.1 比分板界面（核心比赛界面）

参考 `rscampbell333/baseball-scorekeeper` 的 BaseballField 组件：

- **垒上局面可视化**：菱形棒球场图，动态显示跑垒员位置
- **当前打者信息**：头像/姓名、打席数据、当前球数-好球数
- **投手信息**：头像/姓名、投球数、被安打、失分、三振
- **比分板**：显示每局比分、累计总分
- **出局指示**：两个圆圈表示出局数
- **打击顺序表**：当前打者高亮，前一打席结果标注
- **替补席列表**：可点击换人

### 4.2 打席记录表单（PlayForm）

击球结果选择面板，参考：

- `rscampbell333/baseball-scorekeeper` 的 PlayForm 组件
- 选项分组：安打类（全垒打/三垒安打/二垒安打/一垒安打/内野安打）、出局类（三振/接杀/封杀/双杀/高飞牺牲打）、保送类（四坏/触身）、其他（盗垒/盗垒失败）

### 4.3 投手投球计数面板（PitchCount）

- 球数显示：●○○○（好球）/ ●●○○（坏球）
- 当前打席球种分布图
- 投手累计：投球数、被安打、失分、三振

### 4.4 数据报表页面

- **球队排名表**：打击率/OPS/ERA/WHIP 等指标排序
- **球员详情页**：单球员所有统计数据
- **比赛历史**：所有已记录比赛的摘要
- **高级图表**：
  - 喷绘图（Spray Chart）：击球落点分布
  - 投球散点图：好球带内的球种落点

---

## 五、技术架构建议

### 5.1 后端（JavaScript/Node.js）

```
技术栈：Node.js + Express（或其他轻量框架）
数据库：MongoDB（JSON友好，适合棒球嵌套数据结构）
      或 PostgreSQL（适合关系型统计）
```

**数据结构建议**：

```javascript
// 球队
Team { id, name, logo, stadium, players: [PlayerID] }

// 球员
Player { id, name, number, bats: 'L'|'R'|'S', throws: 'L'|'R',
         positions: [], height, weight, birthdate, photo, teamId }

// 比赛
Game { id, date, homeTeamId, awayTeamId,
       innings: [Inning], lineup: Lineup,
       status: 'scheduled'|'in_progress'|'completed' }

// 局面状态（每半局结束时快照）
InningHalf { half: 'top'|'bottom', inningNumber,
             outs: 0|1|2, score: {home, away},
             baseSituation: '---'|'1B'|'2B'|'1-2'|'3B'|'1-3'|'2-3'|'满' }

// 打席记录
PlateAppearance {
  paNumber, batterId, pitcherId,
  result: 'HR'|'2B'|'1B'|'BB'|'SO'|'...',
  rbi: Number, runsScored: Number,
  pitches: [{type, result, zone}],
  notes: String
}
```

### 5.2 前端

```
技术栈：React（推荐）或 Vue.js
状态管理：Redux / Zustand
UI组件库：Tailwind CSS + Headless UI 或 Material UI
图表库：Recharts（React）或 Chart.js
```

### 5.3 数据导出

- 支持导出为 CSV / Excel
- 支持导出为 PDF 格式的比赛报告（Box Score）
- 比赛数据导出为 JSON（方便备份与迁移）

---

## 六、计算引擎设计

### 6.1 实时计算触发点

每次记录打席/投球后，立即触发统计重算：

```
录入打席结果 → 更新球员打数/安打/打点/三振等基础统计
             → 重算 BA/OBP/SLG/OPS
             → 更新投手被打数/失分/三振等
             → 重算 ERA/WHIP

换人操作 → 保存先发投手统计（IP/H/R/ER/BB/SO）
       → 开启新投手统计记录
```

### 6.2 公式实现参考

```javascript
// 打击率
function calcBA(hits, atBats) {
  return (atBats === 0) ? '.000' : (hits / atBats).toFixed(3);
}

// 上垒率
function calcOBP(h, bb, hbp, ab, sf) {
  const denom = ab + bb + hbp + sf;
  return (denom === 0) ? '.000' : ((h + bb + hbp) / denom).toFixed(3);
}

// ERA（自责分率）
function calcERA(earnedRuns, inningsPitched) {
  return (inningsPitched === 0) ? '0.00' : (earnedRuns * 9 / inningsPitched).toFixed(2);
}

// 投球局数（1/3局进制）
function calcIP(totalOuts) {
  const fullInnings = Math.floor(totalOuts / 3);
  const outsInInning = totalOuts % 3;
  return fullInnings + '.' + outsInInning;
}
```

---

## 七、非功能性需求

| 需求 | 说明 |
|------|------|
| 响应速度 | 比赛记录操作 < 100ms 更新到界面 |
| 数据持久化 | 每条打席记录实时保存，支持意外关闭后恢复 |
| 离线支持 | 比赛现场无网络时离线记录，事后同步 |
| 多用户 | 支持主教练/数据员不同角色操作 |
| 移动端适配 | 手机端可完成基本打分操作 |

---

## 八、优先级排序（建议开发顺序）

### P0（核心MVP）
1. 球队/球员 CRUD（增删改查）
2. 阵容管理（Lineup 编辑）
3. 比赛开始与基础打分界面
4. 打席事件记录（PlayForm）
5. 比分板（比分+局面+出局）
6. 基础打击统计计算（BA, OBP, SLG, OPS）
7. 投手统计计算（ERA, WHIP, K）
8. 比赛历史与数据查询

### P1（完善）
9. 投手换人与投球局数精确计算
10. 比赛数据导出（CSV/PDF Box Score）
11. 球员详情页面（完整统计）
12. 球队排名表

### P2（高级功能）
13. 喷绘图（Spray Chart）
14. 投球落点图
15. 移动端离线支持
16. 多用户协作

---

*报告生成时间：2026-05-12*  
*参考来源：MLB Official Rules, Wikipedia (Baseball statistics, OPS), GitHub: rscampbell333/baseball-scorekeeper*
